//! `bitaiird` — BitAiir Core daemon entry point.
//!
//! Today this is a placeholder so the workspace builds end-to-end. Once
//! `bitaiir-node` exposes a runnable node, this binary will:
//!
//! 1. Parse command-line flags and load `bitaiir.conf`.
//! 2. Initialize structured logging.
//! 3. Open the data directory.
//! 4. Start the node and block on a shutdown signal.

fn main() {
    println!("bitaiird: scaffold — node not yet implemented");
}
